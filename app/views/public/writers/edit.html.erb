<h1>マイページの登録情報の編集</h1>
<%= form_with model:@user, url:writer_path(current_user.id), local:true do |f| %>
  <%= f.label :ニックネーム %>
  <%= f.text_field :name %>

  <%= f.label :アイコン画像 %>
  <%= f.attachment_field :image %>

  <%= f.submit '編集完了' %>
<% end %>