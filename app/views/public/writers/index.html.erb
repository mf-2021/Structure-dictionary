<h1>執筆者一覧</h1>
<div>
  <% @users.each do |user| %>
  <section><%= attachment_image_tag user, :image, size:"80x80", format:"jpeg", fallback: "no_image.jpg" %></section>
  <h5><%= user.name %></h5>
  <% end %>
</div>